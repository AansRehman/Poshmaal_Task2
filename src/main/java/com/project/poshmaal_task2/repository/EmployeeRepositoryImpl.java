package com.project.poshmaal_task2.repository;

import com.project.poshmaal_task2.model.Employee;

public class EmployeeRepositoryImpl implements EmployeeRepository{
    @Override
    public Employee changeEmplyeePassword(long id, String password) {
//        TODO
//        The password must be at least 8 characters, contain uppercase and lowercase letters along with digits -
//        "^(?=.*[0-9])(?=.*[a-z])(?=.*[A-Z]).{8,20}$").
        return null;
    }
}
